apiVersion: v2
metadata:
  name: wingspan-scoring
spec:
  services:
    - name: app
      build:
        context: .
        dockerfile: .devcontainer/ContainerFile
      resources:
        memory: 4Gi
        cpu: 2000m
